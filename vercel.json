{
    "version": 2,
    "buildCommand": "npm install -f && npm run build",
    "outputDirectory": "dist",
    "routes": [
      {
        "src": "/(.*)",
        "dest": "./src/index.ts"
      }
    ]
  }
  